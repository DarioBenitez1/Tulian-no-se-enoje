const carreras = require('../../data/carreras.json')

const carrerasMiddle = {}

// seriesMiddle.validaExisteSerie = (req,res,next) => {
//     const id = req.params.id
//     const idx = series.findIndex(s => s.id == id)
//     if(idx < 0){
//         res.status(404).json({mensaje: `no se encuentro el id ${id}`})
//     }
//     next()
// }

module.exports = carrerasMiddle